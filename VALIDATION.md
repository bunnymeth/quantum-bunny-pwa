# PWA Validation Notes

## 2026-08-28：公開預覽載入

公開 HTTPS 預覽網址已成功載入量子兔兔旅行記首頁。已確認顯示「安裝到手機」按鈕、正能量與純淨脫質資源列、量子跳躍入口、次元切換按鈕，以及四個底部導覽頁籤。

第一次預覽因 Vite 公開網域限制被阻擋；已在 `vite.config.ts` 設定 `allowedHosts: true` 並重啟服務，後續載入正常。

## 待驗證

- 已於公開預覽觸發 10 次正向話語；正能量由 0 正確更新為 10，純淨脫質依經過時間即時計算為 2。
- 已以 15／15 的測試資源執行地球與宇宙量子跳躍；成功隨機選到彗星尾巴，正能量扣為 5、純淨脫質扣為 6（扣除後的時間累積值），並正確顯示倒數進行中的旅行狀態。
- service worker 已在公開 HTTPS 網址正確註冊，scope 為 PWA 根路徑。
- 已移除公開預覽中的 1 個舊 service worker 與 1 組舊快取，並重載頁面；新 service worker 已由入口程式碼重新註冊。接著會以全新測試狀態重新驗證單次旅行結算。
- 已以全新的單次完成旅行重測；旅行計數正確為 1，代表結算只寫入一次。重複結算問題已修正。
- 已驗證相簿可顯示京都朱紅鳥居明信片、日期與類型篩選器；點擊卡片後可開啟原創圖片、目的地、寄語與「讓兔兔再次前往」按鈕。
- 已驗證旅行日誌頁會顯示京都朱紅鳥居的縮圖、返抵時間、目的地與自動產生的旅行文字，並提供本機進度重設入口。
- 已載入 100 正能量與 260 純淨脫質的獨立測試進度；首頁正確顯示兩種資源與三個尚未突破的次元入口。
- 已突破微光深海次元壁；純淨脫質由 261 即時扣除為 211，目的地區塊轉為「座標已穩定」並可前往跳躍。此流程沒有使用或改動 APK 資料。
- 已依序突破收容觀測與小小朋友次元壁；純淨脫質分別由 211 降為 111、由 111 降為 11。三個次元最後均顯示「座標已穩定」與可用的原創目的地，連鎖門檻運作正確。
- manifest 正確宣告 `standalone` 顯示模式與 192×192、512×512、SVG 三種圖示；公開 HTTPS 網址的 service worker 已啟用，並快取 PWA 圖示及京都明信片。
- localStorage 已儲存測試進度（100 正能量、11 純淨脫質、三個已解鎖次元）；PWA 重新開啟後仍讀取相同狀態，與 APK 的資料儲存空間獨立。
- 已以正式 `dist` 建置啟動公開 HTTPS 預覽；首頁可正常載入，service worker 已啟用，manifest 的 `standalone` 與 192×192／512×512 圖示均被正確讀取。
- 已以唯讀方式核對使用者提供的 `quantum-bunny-app-v1_0_5.apk`：其為有效 Android APK 封存檔，內含 arm64-v8a 與 armeabi-v7a 原生函式庫；未執行、修改或重新簽署該檔案。
- 離線優化初測：新版 service worker 已啟用，核心快取含 26 項資源（6 個 App Shell／圖示資源與 20 張原創明信片）。另發現首次更新時舊 service worker 可能先接收資源快取訊息，已補上控制權切換後的重新快取，確保新版編譯 JavaScript 與 CSS 也會寫入離線快取。
- 更新後檢查：`quantum-bunny-offline-v3-core` 含 26 項核心資源（`index.html`、manifest、圖示與 20 張明信片）；`quantum-bunny-offline-v3-runtime` 含 8 項執行期資源，確認編譯後 JavaScript、CSS 與 `index.html` 均可從快取取得。本機旅行狀態已存在於 localStorage。
- 公開入口已恢復且新版首頁顯示「離線旅程已備妥」。由於暫停沙盒的公開轉送服務會讓該轉送層先回傳不可用頁，不能作為瀏覽器離線行為的有效模擬；改以瀏覽器本身的網路離線模式執行可重複驗證。
- 瀏覽器離線模式回歸測試通過：在網路完全停用時重新載入，已快取首頁正確顯示「量子兔兔旅行記」與「離線旅程已備妥」；離線點擊正能量按鈕後，本機旅行進度也正確寫入 localStorage。
- 探索、相簿、旅行日誌與次元牆流程。
- manifest 與 service worker 註冊、離線快取行為。

## 2026-08-29：沙盒重置後入口恢復

- 沙盒重置後正式 `dist` 曾遺失；已恢復 `src/data.ts`、20 張已壓縮原創明信片、`index.html` 與 CSS 型別宣告，並重新通過 `pnpm build`。
- 新正式預覽服務已在 4173 連接埠回應 HTTP 200，公開入口為 `https://4173-i0c32sjzs6c2fanswd972-cfa3cb0b.sg1.manus.computer/`。
- 公開 HTTPS 頁面可正常載入「量子兔兔旅行記」首頁；manifest 存在，service worker 狀態為 `activated`。失效畫面是舊臨時網址／服務睡眠，不是 Android Chrome 設定問題。
- 已完成新入口驗證；使用者手機仍須重新開啟新網址一次，首次開啟後等待離線就緒狀態，再選擇加到主畫面。
